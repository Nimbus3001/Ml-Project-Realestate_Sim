## Setup

1. Clone repo
2. Create virtual environment
3. Install dependencies:

pip install -r requirements.txt

4. Add dataset manually to:
data/raw/

5. Train model:

python -m ml.train_model

6. Run prediction:

python -m ml.predict